# Motion Studio 编程代理指南

本文件适用于整个仓库。它面向接手本项目的新编程代理，用于快速恢复项目背景、保持既有架构和物理口径，并完成可验证、可回退的修改。

## 1. 开始工作前

每次收到修改请求后，先完成以下检查，再动代码：

1. 阅读用户当前请求；如果用户已经确认详细方案，以该方案为本次验收基线。
2. 运行 `git status --short`，确认当前分支和未提交修改。已有修改默认属于用户，不得覆盖、回退或顺手清理。
3. 用 `rg` 定位实际实现、测试和相关 ADR，不根据旧对话猜文件名或 API。
4. 阅读与任务直接相关的 `PROJECT_SPEC.md` 小节、`docs/PHYSICS_VALIDATION.md` 和 `docs/adr/`；不要为了一个局部任务重读或重写全部文档。
5. 修改前用 3～6 条中文说明将要改变的文件、行为和最快验证方法。

当前请求若与本文件冲突，以用户本次明确要求为准；但必须主动指出可能破坏存档兼容性、物理规律、数据安全或既有测试的风险。

## 2. 当前基线

- 产品：Motion Studio，二维物理运动编辑与可视化工具。
- 当前应用版本：`1.5.4`。
- 当前场景格式：`schemaVersion: 16`；格式 15 无损提升格式号，格式 12、13、14 依次迁移，格式 11 或更早拒绝读取。
- 当前仓库是 React/Vite Web 主项目，并包含 Tauri v2 桌面版封装（`src-tauri/`、`.motionstudio` 文件关联、原生窗口菜单与最近文件）。桌面文件流程以 `src/persistence/sceneFileService.ts` 为边界，平台差异见 `docs/adr/0030-tauri-desktop-files-and-system-integration.md`。
- 技术栈：TypeScript、React、Zustand、PixiJS、Rapier 2D、ECharts、Zod、Vitest、Playwright、gifenc、polygon-clipping、earcut。
- Node.js 要求：`>=22`；包管理器：`pnpm@11.9.0`。
- 开发服务器通常为 `http://127.0.0.1:4173/`。
- 最近一次完整验收快照（2026-08-21，v1.5.3，提交 `a33c3e8`）：`pnpm check` 通过 446 项 Vitest 测试，`pnpm e2e` 通过 49 项 Chromium/视觉用例；32 个独立布尔结果提交、完整 CSG 重算、移动预览、空差集和 8 个复合刚体固定步 P99 分别为 `1.6305 ms`、`79.1552 ms`、`0.0201 ms`、`0.3064 ms` 和 `0.6320 ms`。32 叶刚体快速提交的均值/P75 为 `0.6559/0.4461 ms`，但完整串行 P99 为 `12.7937 ms`，严格 `<8 ms` 目标仍未稳定达标；完整串行综合物理场景 P99 为 `9.1962 ms`，均值/P75 为 `3.7588/4.3464 ms`。所有尾部数据均保留，不得放宽阈值、删样本或把它们误报为达标。这些数字只用于回归比较，每次任务仍须以实时结果为准。

版本号和测试数量会继续变化。开始任务时应从 `package.json`、`src/scene/model/types.ts` 和实时测试结果重新确认，不能把本节当作永久事实。

## 3. 权威资料

按以下用途查阅资料：

- `PROJECT_SPEC.md`：产品行为、物理口径、数据模型、架构边界和阶段验收。
- `README.md`：运行方式、现有能力和当前目录结构。
- `docs/PHYSICS_VALIDATION.md`：必须守住的物理规律、误差标准和已知近似。
- `docs/PERFORMANCE_BASELINE.md`：性能场景、P99 目标和 Windows 调度离群的记录方式。
- `docs/adr/`：已经作出的技术决定及其取舍。改变核心物理、存档、Worker 协议或编辑器架构前，先检查对应 ADR。碰撞绳当前以 ADR 0026 和 ADR 0027 为准；布尔 CSG 与复合物理以 ADR 0028 为准；统一场景树和结果级分布覆盖以 ADR 0029 为准。不要恢复更早的补丁式接触方案。
- `CONTRIBUTING.md`：Git 提交和最低检查要求。

若实现与文档不一致，先通过测试和代码查明真实行为。不要静默选择一边；修复后应同步更新过时文档。

## 4. 目录与职责

- `src/app/`：应用外壳、顶层命令协调和错误边界。
- `src/editor/`：命令、撤销/重做、剪贴板、几何、吸附和待提交编辑协调。
- `src/features/`：画布、工具栏、场景树、属性、图表、GIF 导出和可停靠工作区。
- `src/persistence/`：场景打开、保存、校验、草稿和导出文件封装。
- `src/physics/core/`：可独立测试的物理世界、碰撞、场力、曲面路径和连接器求解。
- `src/physics/core/rope/`：碰撞绳的粒子状态、统一 XPBD 长度/接触约束、持久接触流形和局部连续扫掠。
- `src/physics/client/`：主线程与物理 Worker 的控制器。
- `src/physics/worker/`：固定步循环、消息协议和运行时采样。
- `src/physics/recording/`：GIF 等独立历史记录。
- `src/renderer/`：PixiJS 场景、地面和编辑辅助图形渲染。
- `src/scene/model/`：场景类型、实体工厂、地面路径和默认值。
- `src/scene/validation/`：Zod 存档校验。
- `src/scene/migrations/`：旧场景逐版本迁移。
- `src/stores/`：文档、编辑器、模拟、图表和工作区布局状态。
- `src/styles/tokens.css`：颜色、尺寸和层级等全局设计令牌。
- `e2e/`：Playwright 浏览器交互与视觉回归。

依赖方向必须保持：场景模型不依赖 React/Pixi/Rapier；物理核心不依赖 React；渲染器不直接修改文档；UI 不直接持有 Rapier 句柄。

不要建立含义模糊的通用 `utils` 文件。纯函数优先放在所属业务模块附近，并在同目录添加测试。

## 5. 三类状态必须分开

修改前先判断数据属于哪一类：

1. **文档状态**：需要保存、撤销/重做的场景和图表配置。通过命令层修改，并标记文档未保存。
2. **编辑器状态**：选择、当前工具、悬停、拖动预览、吸附开关、面板布局等；只有确有产品约定时才持久化。
3. **运行时状态**：Rapier 句柄、路径接触、模拟帧、图表/GIF 样本等；不得直接写回场景存档。

特别注意：

- `visible` 只控制显示，`simulationEnabled` 控制是否参与模拟，两者不能互相替代。
- 删除实体必须使用可撤销命令，并级联处理依赖连接器或地面连接点，再清理失效选择和工具临时状态。
- 拖动期间只更新实时预览；松开时生成一条原子撤销命令，取消时恢复原状态。
- 场景选择变化、标签切换、菜单操作或程序化选择之前，必须先提交合法的属性/名称草稿；非法草稿恢复原值。相关协调逻辑在 `src/editor/editing/pendingEditorEdit.ts` 及顶层调用处。

## 6. 坐标、单位和数值

- 物理和存档统一使用 SI：米、秒、千克、弧度、牛顿、库仑、特斯拉。
- 世界坐标 X 向右、Y 向上；屏幕坐标 Y 向下。转换只能通过统一视口/相机函数完成。
- 像素只用于界面阈值和渲染，不能直接进入物理公式。
- 字段名应标明容易混淆的单位，例如 `massKg`、`angleRad`、`radiusM`。
- 外部输入、存档和 Worker 消息必须做运行时校验；不得让 `NaN`、无穷值或非法尺寸进入场景。
- 重要容差、采样上限和软化参数必须使用命名常量，并说明物理或数值原因。

## 7. 物理核心不可破坏的约定

### 固定时间线

- 对外固定步长为 `1/120 s`；播放倍速通过执行更多固定步实现，禁止增大 `dt`。
- 弹簧与高曲率路径可以触发统一内部子步，整个物理世界必须处在同一时间线上。
- 内部子步上限为 32；超出能力时发出明确警告，不得隐藏限速、裁剪能量或偷偷增加阻尼。

### 材料与碰撞

- 两接触面的摩擦系数使用 `sqrt(μ1 × μ2)`。
- 两接触面的弹性系数使用 `sqrt(e1 × e2)`；任一方为 0 时不主动反弹。
- 地面是双面碰撞面。小球使用标准双面路径碰撞；物块使用来自同一解析路径的局部薄接触带。
- 连接器只响应标准地面路径，不重复加入物块专用地面组，避免双重冲量。
- 完全弹性、无摩擦、无外力的固定地面碰撞只能改变速度方向，不能改变动能；有外力时机械能变化必须与外力做功一致。
- 加速度和合力来源于真实物理步的动量变化，不得用图表滤波掩盖求解尖峰。

### 统一地面路径

- 直线、圆弧、贝塞尔和地面连接过渡统一通过 `src/scene/model/groundPath.ts` 提供弧长、位置、切线、法线、曲率、最近点、裁剪与采样。
- 渲染、碰撞采样和小球持续接触必须共享该路径网络，禁止各自推导另一套几何。
- 自动连接使用紧凑的对称裁剪：`0.4 × 较短地面长度 × sin(夹角/2)`，按有限缩放序列选择首个安全五次曲线。不要恢复昂贵的非对称全局候选搜索。
- 重合且符合条件的端点使用 `kind: 'direct'` 无形接缝：只建立路径邻接，不增加曲线、裁剪或碰撞体。
- 持续路径接触只可由真实 Rapier 接触激活；预测接触不能把已离轨小球重新吸回路径。
- 负支持力、主动法向分离、无连接端点或偏置曲率无解时必须立即离轨。
- 高速接缝修复不能靠限速完成；要检查跨段剩余时间、相邻路径碰撞屏蔽和能量误差。

### 布尔复合体与圆槽接触

- 布尔树只保存来源和运算关系；派生轮廓、材质/质量区域、三角网格、凸片和 Rapier 句柄都是运行时数据，不得写入存档。渲染、命中、吸附和物理必须共享 `src/scene/boolean/` 的解析结果，不能各算一套 CSG。
- 同材质凸片必须合并为共享 Float32 顶点和三角索引的网格，用于低速及持续接触；不得恢复“每个凸片一个碰撞体”的实现，否则内部拼接边会产生假法线、弹跳和卡顿。凸片代理只用于确有薄障碍穿透风险、命中新障碍且尚无接触的高速 CCD 阶段。
- 普通小球只接触 CSG 的真实外环、孔洞和多岛边界，不接触三角网格内部边。真实 Rapier 接触建立后，圆形来源边界可以进入解析持续接触；主动分离、负支持力或离开有效边界时必须立即释放，禁止预测回吸。
- 不足完整一周的解析圆弧具有真实物理端点。持续状态在一个固定步内越过端点时，必须先把广义坐标钳制到精确端点，以端点切线和约化动量向双方完成最后一次等大反向投影，再主动离轨；在离开端点净空前禁止由顶点流形立即重捕获。完整圆覆盖整条闭合路径时首尾只是采样接缝，必须继续循环，不能触发端点释放。
- 可移动四分之一圆滑道的竖直切线出口必须守住双方水平速度一致。标准回归为 `1 kg` 小球以 `15 m/s` 沿无摩擦、零弹性的 `5 kg` 滑道运行 720 个固定步：水平总动量误差 `<0.01 kg·m/s`，离轨水平速度差 `<1e-6 m/s`，共同速度约 `2.5 m/s`；小球返回尖端时仍须由真实 Rapier 流形重新进入持续接触。
- 有限质量布尔圆槽同时受地面和小球作用时，必须从槽体自由度中投影地面法向；平底在质心两侧形成真实多点支撑时还要承接转动力矩，不能先让槽体穿地、再靠下一步碰撞消能。单点支撑仍允许物理上合理的翻转。
- 无摩擦圆槽沿地面保留水平自由度。小球弧长与槽体水平位移必须按双方质量和转动惯量组成约化系统，并以隐式中点推进；法向、切向的位置与速度校正要联立求解，向双方施加等大反向响应。
- 不得用隐藏阻尼、限速、能量裁剪、恢复系数补偿、图表滤波或额外子步乘法掩盖圆槽问题。有限质量平底圆槽的回归至少运行 2400 个固定步，并守住：机械能摆幅及首末转折高度误差均 `<0.5%`、最低实体边界不低于地面 `-0.002 m`、总水平动量误差 `<0.01 kg·m/s`，且多点支撑不发生被小球压着旋转穿地。

### 弹簧、杆和旋转

- 阻尼为 0 表示真正无阻尼，不设置隐藏最小值。
- 弹簧弹力使用二阶速度 Verlet 的前后半冲量；不要退回“步首计算一次整步弹力”。
- 高刚度弹簧按 `ωΔt_sub ≤ 0.1` 选择统一子步。
- 弹簧质量固定为 `0 kg`，普通连接器碰撞固定关闭；自由端弹簧通过轴向挡板与物体或地面接触，不得把它重新实现为带质量、带普通胶囊碰撞的柔性链。
- 小球和物块关闭旋转时使用 Rapier 转动自由度锁定；所有自定义角速度写入也必须保持为 0。
- 无质量杆的距离约束必须计入平动质量、锚点力臂和转动惯量；杆 A/B 两端的自由/固定转动是独立语义。
- 连接器端点可为物体、地面/连接段；弹簧还允许自由端和显式固定世界点。弹簧工具点击空白处默认创建自由端，只有“固定到当前位置”才创建固定世界点。
- 新建连接器时点击物体默认使用物体中心，但旧偏心锚点不得被迁移或强制居中。
- 杆等无质量碰撞代理与地面接触时，使用子步前后代理段扫掠并把冲量传回真实端点；两端固定时不能凭空移动。

### 碰撞绳 XPBD

- 开启碰撞的绳使用粒子绳和连续胶囊段，质量下限为 `0.001 kg`；关闭碰撞的绳保持 `0 kg` 的理想最大距离约束，不进入碰撞绳求解器。
- 每轮位置求解的固定顺序为：保存本轮位置 → 求解逐段长度、长距离张力和总弧长 → 只对被长度约束移动过的绳段做局部连续扫掠 → 建立或刷新接触 → 最后执行接触投影。最后一个位置操作必须是接触投影。
- 绳长误差和穿透误差必须同时达标才能提前退出。禁止在统一 XPBD 循环结束后再执行一次不考虑接触的完整绳长投影；这种投影会把刚推出障碍物的节点重新拉穿。
- 位置约束全部完成后只重建一次绳节点速度，再执行法向、摩擦和拉伸速度约束。不得同时用位置重建、端点预分配冲量和额外速度补丁重复计入同一次碰撞。
- 绳—物体接触首先作用于真实接触段的相邻节点，再由不可伸长约束把张力传到端点。存在刚体接触时，整绳总长度修正只能通过真实端点传力；纯地面接触可让总长度约束调整内部节点。
- 持久接触流形必须保存进入侧、参考法线和接触来源。只有真实物理扫掠建立的新接触可执行一次弹性响应；仅由长度投影发现的接触只阻止穿透。间隙超过 `0.004 m` 且沿原法线分离后才释放接触。
- 圆球使用解析胶囊—圆距离；旋转物块使用 Rapier 的精确接触点，并以凸体支撑平面判断进入侧，不能用“物块中心到绳段最近点”近似替代角点或边接触。
- 位置迭代预算使用 `clamp(4 × 自适应分组数, 8, 32)`。达到 32 次仍无可行解时，优先保持非穿透并报告剩余绳长误差，不得限速、裁剪能量或加入隐藏阻尼。
- 物理验收至少守住：残余穿透不超过 `0.002 m`；总弧长不超过 `maxLength + max(1e-4 m, maxLength × 1e-4)`；不得粘球、回吸、沿绳持续增速、突然归零或产生 NaN。
- `RuntimeConnectorState.points` 是画布、命中检测和 GIF 的共同运行时折线；XPBD 粒子、累计乘子和接触流形只能保存在运行时，不能写回场景。

对任何物理修复，先写能复现错误的测试，再修改求解器。不能通过调宽能量阈值、平滑曲线或删除失败样本使测试“通过”。

## 8. 存档与兼容性

- 当前版本常量位于 `src/scene/model/types.ts`。
- 当前 `schemaVersion: 15` 只接受 `rootItems` 递归场景树；格式 14 无损迁移，格式 12、13 通过确定性迁移继续提升，格式 11 或更早的 `layers` 结构直接拒绝。布尔节点的稳定 `resultId` 可被连接器和图表引用，但运行时几何、凸片和缓存修订仍不得进入存档。
- `rootItems` 数组首项同时是面板最上方和画布最前方。普通实体或布尔节点在整棵树中必须恰好出现一次；布尔节点最多两个有序输入，最大深度 64，不允许共享或循环。
- 布尔结果的质量、电荷、摩擦、弹性、初速度和初角速度分别保存来源模式或结果级覆盖。修改其中一项不得改动其他项；质量和电荷的均匀分布按真实 CSG 面积计算并排除孔洞。
- 场景结构变化必须同时更新：类型、实体工厂默认值、Zod 模式、迁移、往返测试、README/规范和版本号。
- 需要新增字段时升级 `schemaVersion`；若产品明确采用断代格式，则应删除对应迁移路径并严格拒绝旧结构，否则实现逐版本迁移。新版本缺失必填字段应拒绝读取。
- 仅改变新建默认值且旧数据可原样保留时，通常不需要迁移，但必须添加“新对象默认值”和“旧存档值不被覆盖”测试。
- 旧场景中已有电荷、阻尼、锚点、材料、可见性和连接设置必须原样保留，除非用户明确批准迁移。
- 图表/GIF 的运行采样不写入场景；只有明确的配置进入存档。
- 文件读取继续遵守大小限制、JSON 解析、迁移和 Zod 校验。坏文件不得覆盖当前文档。

## 9. 编辑器和界面约定

- 文档变更走命令和撤销栈；悬停、预览、播放头和临时手柄不进入撤销栈。
- 对象或场景树节点切换前先处理正在编辑的输入，避免修改丢失或草稿串到另一个实体。
- 工具命中、吸附预览和最终提交必须调用同一几何算法，避免鼠标松开后位置跳变。
- `Alt` 是吸附的临时绕过键；多选移动保持内部相对位置。钢笔物块和钢笔有限场的控制棒默认镜像联动，按住 `Alt` 拖控制棒时只移动当前一侧；`Alt+左键单击`锚点可折叠或精确恢复控制棒。
- 布尔整体平移和旋转预览及提交应优先走稳定内容修订缓存与刚体快速路径，不得执行 CSG；混选普通实体和根布尔结果时先展开来源并按 ID 去重，松手只生成一条原子命令。从场景树显式选择的布尔来源可单独变换，但父结果优先且父锁定递归生效。
- 钢笔物块和三个曲面预设允许闭合无自交的凹轮廓，使用与布尔结果相同的面积、质心、凸片和共享索引网格接口；无效轮廓不得生成实体或阻塞其他物体模拟。
- 节点隐藏不停止物理；模拟运行时按既有规则锁定几何编辑。
- 浮层层级统一放在 `src/styles/tokens.css`：普通内容 < 固定栏 < 浮动面板 < 停靠预览 < 工具悬浮窗 < 菜单 < 模态窗口 < 通知。不要在局部随意堆叠巨大 `z-index`。
- 模态导出期间必须锁定其他工作区和快捷键，并处理焦点限制、取消及资源释放。
- 图表显示数字使用统一格式器；常量数据必须扩展为稳定的小范围，不能让坐标轴因浮点尾数来回伸缩。
- 新图标优先使用项目现有 `lucide-react`，并检查语义、尺寸和无障碍名称。

## 10. 实现一个功能时的覆盖面

根据任务类型检查以下层，不要机械地全部修改：

1. 场景类型与默认工厂；
2. 存档校验与迁移；
3. 编辑命令、撤销/重做和剪贴板关系；
4. 工具创建、命中、吸附与控制点；
5. 属性面板和待提交草稿；
6. Pixi 渲染和选择覆盖层；
7. Rapier/自定义物理及 Worker 消息；
8. 图表、GIF 和运行时采样；
9. 单元、物理、浏览器和视觉测试；
10. `PROJECT_SPEC.md`、物理验证、性能基线和 ADR。

如果一个新实体需要在许多 `switch` 中重复补分支，先检查 `src/scene/registry/entityRegistry.ts` 和现有可辨识联合，优先沿当前扩展方式实现，不进行无关的大规模重构。

## 11. 测试与验收

优先从最快、最相关的检查开始：

```powershell
pnpm test -- <相关测试文件>
pnpm typecheck
pnpm format:check
```

行为改动完成后的标准验收顺序：

```powershell
pnpm format:check
pnpm check
pnpm e2e
pnpm benchmark
```

说明：

- `pnpm check` 包含 ESLint、TypeScript、全部 Vitest 测试和生产构建。
- `pnpm e2e` 使用 `scripts/run-e2e.mjs` 显式启动/停止 Vite；在 Windows 上比依赖 Playwright `webServer` 协调更稳定。
- 首次运行浏览器测试前执行 `pnpm e2e:install`。
- 视觉变更只有在确认产品行为正确后才能更新快照；不得用更新快照掩盖布局回归。
- 物理固定步 P99 目标为 `<8.33 ms`，普通编辑/图表交互反馈目标为 `<100 ms`。
- Windows 后台任务可能产生 20～50 ms 甚至更高的调度离群。应保留原结果、隔离复测受影响场景并记录 P99 趋势；不得删除样本或放宽目标。
- 长时间物理测试若只因系统负载超过默认测试超时，可为该测试声明合理超时；不得减少模拟时长或放宽物理断言。

不同改动至少需要：

- 几何/纯函数：参数化单元测试和退化输入。
- 物理：无外力守恒、有外力做功、正反方向、低速/高速和接触/脱离测试。
- 编辑交互：预览、提交、取消、撤销/重做和对象切换测试。
- 存档：当前版本往返、按产品约定迁移或拒绝旧版本、非法文件拒绝。
- UI：宽屏、`1024×720`、窄窗口以及键盘/无障碍检查。

## 12. Git 与文件安全

- 重大功能、架构调整、场景格式变化或高风险物理修复完成后创建独立 Git 提交。
- 提交前检查 `git diff --check`、测试结果和 `git status --short`；只暂存本次相关文件。
- 推荐提交格式：`feat: ...`、`fix: ...`、`docs: ...`、`test: ...`、`refactor: ...`、`chore: ...`。
- 不使用 `git reset --hard`、`git checkout --` 或其他可能抹掉用户修改的命令。
- 不提交密钥、Token、`.env`、个人路径数据或场景隐私内容。
- `node_modules/`、`dist/`、`playwright-report/`、`test-results/`、日志和本地缓存是生成物，不在代码中引用，也不提交。只有用户明确要求清理时才删除。
- 不修改锁文件，除非依赖确实变化；新增依赖前说明必要性、许可证和体积影响。
- 用户要求“保存 Git 版本”时，应完成验证并真正提交，不能只给出命令。

## 13. 高频故障检查

- **小球在曲面接缝弹起、折返或能量异常**：检查解析路径与 Rapier 折线是否重复施加冲量、接缝邻接是否存在、法线方向和剩余时间是否正确；同时比较有/无连接点、有/无重力和不同速度。
- **小球离轨后被吸回**：检查路径状态是否由预测接触重新激活，以及支持力、主动分离速度、端点净空和同一物体对的短期重捕获屏蔽。
- **小球离开可移动圆弧后水平速度略大于滑道**：检查固定步是否越过开放解析圆弧端点后直接释放。正确顺序是钳制到精确端点、按端点切线完成约化动量投影、标记主动离轨；同时确认完整圆采样接缝没有被误判为开放端点。
- **物块在连接地面卡顿**：检查碰撞链是否保留了内部共线顶点或端帽，不能用小球路径约束掩盖物块碰撞问题。
- **布尔复合体平移时突然弹起或卡顿**：检查是否误把每个凸片建成独立碰撞体、共享网格是否仍含重复/退化三角形，以及普通小球是否碰到了 CSG 内部三角边。持续接触不得在共享网格和凸片代理之间反复切换。
- **无摩擦布尔圆槽往复衰减、增能或被压穿地面**：先检查地面法向自由度投影、多点支撑转动约束、圆槽—小球约化质量和法/切向联立响应；分别比较固定槽、有限质量槽、开启/关闭旋转和有/无地面。不能通过增加摩擦/阻尼、调整弹性或平滑图表处理。
- **无阻尼弹簧增能**：确认弹性半冲量对称、子步一致、质量属性已刷新；不要加入隐藏阻尼或能量边界。
- **连接器能撞物体却穿过地面**：无质量连接器需要显式扫掠地面接触，运动学代理不会从固定地面自动获得有效反作用；有质量连接器检查碰撞组是否包含标准地面组。
- **碰撞绳穿过小球或物块**：先检查统一 XPBD 循环之后是否又执行了脱离接触的绳长投影；所有长度修正后都要局部重扫被移动的绳段，并以接触投影结束本轮。旋转物块还要检查精确接触点、支撑平面和进入侧。
- **碰撞绳粘在球面、回吸或沿绳持续增速**：检查持久流形的释放条件、参考法线和接触来源；弹性只能来自真实新扫掠，节点速度只能统一重建一次，不能叠加端点预分配冲量或重复速度补丁。
- **碰撞绳长度与非穿透无法同时收敛**：区分纯地面接触和刚体接触的总长度修正路径；动态端点的圆球接触至少执行正反两个扫描方向。达到 32 次预算后优先非穿透并发出包含实体名和剩余长度误差的警告。
- **属性修改后切换对象丢失**：检查捕获阶段的待提交编辑协调，不要只依赖 React 普通 `blur` 顺序。
- **浮窗被面板遮挡**：先检查全局层级令牌和层叠上下文，不在局部不断增加数字。
- **E2E 启动超时**：使用项目脚本 `pnpm e2e`，不要自行留下后台 Vite 进程。
- **性能测试偶发超标**：先隔离场景复测并记录 Windows 负载；看平均值、P75、P99 和最大值的共同变化，不主观宣称达标。

## 14. 完成标准与交付说明

任务完成必须同时满足：

1. 用户要求的行为已经实现，而非只写方案或隐藏症状；
2. 正常、边界和回归路径具有相称的测试；
3. 物理修改通过明确的守恒/耗散/约束验收；
4. 存档、撤销、运行时和显示语义没有混淆；
5. 相关规范、验证文档、性能记录或 ADR 已同步；
6. 已运行与风险相称的检查，并如实报告任何未通过或环境性离群；
7. 用户要求时已创建目的清晰的 Git 提交，工作区没有意外修改。

最终回复默认使用中文，先说明结果，再列关键改动文件、验证结果、Git 提交和仍存在的边界。面向初学者解释时使用清楚的产品语言，但不能省略会影响物理正确性或数据兼容性的技术事实。
