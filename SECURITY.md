# 安全策略

## 支持范围

安全修复优先面向当前 `main` 分支和最新公开版本。场景文件可能来自不可信来源；请仅使用最新版本打开外部场景。

## 报告漏洞

请通过 GitHub 仓库的 Security 页面创建私密安全报告。请不要在公开 Issue 中披露可复现利用细节。报告中请包含受影响版本、操作系统、复现步骤、最小样例和预期影响；维护者会在确认后协调修复与公开时间。

## 场景文件安全边界

- `.motionstudio`、`.motion.json` 和 `.json` 都按不可信 JSON 数据处理。
- 文件读取限制为 10 MB，并在解析后执行版本迁移与 Zod 数据校验。
- 公式由受限语法树解释，不执行 `eval`、脚本或场景内网络请求。
- 桌面前端只持有 Rust 生成的不透明文档令牌，不能用任意路径读取本机文件。
- 桌面版不启用 Shell、HTTP、遥测或宽泛文件系统权限。

如果场景文件触发任意代码执行、越权文件访问、资源上限绕过或静默网络请求，请按安全漏洞报告。
