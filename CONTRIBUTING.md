# 开发与版本管理约定

## Git 基线

- 每一项重大功能、架构调整、数据格式变更或高风险修复完成后，都应创建一个独立 Git 提交；
- 提交前至少运行与改动相关的测试；影响全局构建时运行 `pnpm check`；
- 不提交 `node_modules`、构建产物、测试报告、本地工具、日志、密钥或 `.env` 文件；
- 提交说明应描述完成的结果，例如 `feat: 完成阶段 2 刚体与地面模拟`；
- 小型连续修正可以在验证完成后合并为一个有明确目的的提交。

## 推荐流程

```powershell
git status
pnpm check
git add <本次相关文件>
git commit -m "类型: 简要说明"
```

常用提交类型：`feat`（功能）、`fix`（修复）、`docs`（文档）、`test`（测试）、`refactor`（重构）和 `chore`（工程维护）。
