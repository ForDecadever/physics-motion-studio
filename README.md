# Motion Studio

一个采用专业图形编辑器布局的二维物理运动模拟工具。当前版本为 `0.8.0`，已在 [PROJECT_SPEC.md](./PROJECT_SPEC.md) 的阶段 4 基础上形成可编辑、可模拟、可记录和可保存的第一版。

## 当前已经具备

- React + TypeScript + Vite 工程和 Photoshop 风格编辑布局；
- PixiJS 二维画布、自适应米制网格、平移、缩放、选择、移动、旋转与吸附；
- 创建统一的带电/可碰撞小球、物块、直线地面、圆弧地面和可拖控制柄的贝塞尔地面；
- 用“地面连接点工具”依次选择两个可相隔一定距离的语义端点：`≤1°` 拒绝连接，`1°～179°` 生成安全的五次平滑过渡，`≥179°` 直接生成端点间直线；新地面的起点靠近未占用端点时可自动建立连接；
- 显示、碰撞采样和小球持续接触共用统一地面路径；连接只消除接缝假冲量，所需支持力为负时小球仍会自然脱离；
- 墙面吸附按鼠标所控制的物体中心到最近地面点的真实欧氏距离判断，沿地面延长线远离端点时不会被强制吸回；
- 绘制矩形、圆/扇形、钢笔自由形状或无限范围的匀强重力场、电场和磁场，并显示方向图例；
- 计算物体间库仑力，并可在场景属性中关闭或开启（新场景默认关闭）；
- 使用绳、杆或弹簧连接两个物体，直接选择连接件并拖动两端锚点；
- 在右侧编辑位置、角度、尺寸、质量、初速度、初角速度、旋转约束、摩擦系数、弹性系数和 CCD；
- Rapier 2D 刚体碰撞在独立 Web Worker 中运行，不会阻塞界面；
- 固定 `1/120 s` 物理步长，以及播放、暂停、单步、倍速和重置；
- 圆弧和贝塞尔曲线自适应离散；每块地面在 Rapier 中使用一个原生双面 `polyline` 碰撞体；
- 接触摩擦系数和弹性系数均按几何平均合成，分别为 `sqrt(μ₁ × μ₂)` 与 `sqrt(e₁ × e₂)`；
- 选中运行中的物体时显示轨迹、速度和物理步内平均合外力；
- 同时记录最多 10 条多物体物理量曲线，默认以模拟时间 60 Hz 保留最近 300 秒；
- 导出带指标和单位表头的 CSV，曲线支持隐藏、移除、缩放和同刻悬浮提示；
- 新建、打开、直接保存/另存或下载 `.motion.json` 场景，写入后执行 SHA-256 内容校验；
- IndexedDB 自动草稿、意外刷新恢复，以及格式 1 到当前格式 6 的逐级纯函数迁移；格式 3 的旧单面地面会迁移为双面碰撞，格式 4 的连接点会补齐自动过渡设置，格式 5 的物体会默认补齐旋转开关；
- 复制/粘贴、播放快捷键、键盘可调面板、减少动态效果和明确焦点样式；
- ESLint、Prettier、TypeScript、Vitest、Playwright 视觉回归和物理性能基准。

物理验证方法与误差边界记录在 [docs/PHYSICS_VALIDATION.md](./docs/PHYSICS_VALIDATION.md)，性能结果记录在 [docs/PERFORMANCE_BASELINE.md](./docs/PERFORMANCE_BASELINE.md)。

## 第一次运行

打开 PowerShell，进入项目文件夹：

```powershell
cd "C:\Users\FD\Desktop\chengxu\题目可视化"
```

安装依赖：

```powershell
pnpm install
```

启动开发界面：

```powershell
pnpm dev
```

成功时会显示 `http://127.0.0.1:4173/`。终止服务器时按 `Ctrl+C`。

## 如何搭建第一个运动场景

1. 用“地面工具”拖出地面；
2. 用“物体工具”放置小球或物块；
3. 用“场工具”画出覆盖物体运动范围的重力场；
4. 选中物体，在右侧输入初速度等参数；
5. 点击底部播放按钮。模拟开始后编辑会锁定，点击重置回到 `0 s` 后可继续修改。

## 检查命令

完整检查：

```powershell
pnpm check
```

真实浏览器交互检查：

```powershell
pnpm e2e
```

成功时会看到全部测试通过。第一次在新电脑上运行浏览器检查前，需要先执行 `pnpm e2e:install`。

物理性能基准：

```powershell
pnpm benchmark
```

## 主要目录

```text
src/physics/core/        可独立测试的物理世界、曲线采样和场区域判断
src/physics/worker/      后台物理循环和主线程消息协议
src/physics/client/      界面与物理线程之间的控制器
src/features/            菜单、工具栏、画布、属性、播放和图表区域
src/scene/               场景数据、校验、迁移和实体工厂
src/stores/              文档、编辑器和运行时状态
docs/                    物理验证报告和架构决策记录
e2e/                     真实浏览器测试
```

## 下一阶段

阶段 5 将在 Web 第一版稳定后评估 Tauri 桌面封装、最近文件、更多物体/场/连接器，以及独立设计的 3D 模式。
