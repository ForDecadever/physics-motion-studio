# Motion Studio v1.5.3 物理正确性验证

更新日期：2026-08-15

## 1. 验证目标

v1.5.3 的验收标准不是“动画看起来像真的”，而是相同初始条件下，普通刚体、预设/钢笔物块、布尔复合体、统一地面路径、场和连接器的数值结果应在明确误差范围内符合经典力学，并且运行结果可复现。

内部统一采用 SI 单位：长度为米 `m`、质量为千克 `kg`、时间为秒 `s`、角度为弧度 `rad`。物理核心固定以 `Δt = 1/120 s` 前进；播放倍速只改变单位现实时间内执行的固定步数，不改变 `Δt`。

## 2. 自动验证项目

| 项目 | 对照规律或目标 | 自动验收条件 |
| --- | --- | --- |
| 固定步长 | `t = N × Δt` | 120 步后模拟时间为 1 秒，误差小于 `1e-12 s` |
| 自由落体 | `v = gt`、`y = y₀ + 1/2 gt²` | 速度与解析解误差小于 `0.001 m/s`；位置允许固定步长积分误差 |
| 斜抛运动 | `x = v₀x t`、`y = v₀y t + 1/2 gt²` | 水平速度保持不变，水平和竖直位置落在规定容差内 |
| 质量无关性 | 同一重力加速度下不同质量物体运动相同 | `0.5 kg` 与 `8 kg` 物体的位置和速度在 6 位小数内一致 |
| 完全弹性正碰 | 动量守恒、动能守恒 | 等质量物体交换速度；总动量和总动能落在规定容差内 |
| 弹性合成 | `e = sqrt(e₁ × e₂)` | `e₁=0.25`、`e₂=1` 时接触恢复系数约为 `0.5`；任一侧为 `0` 时不主动反弹 |
| 滑动摩擦 | `a ≈ μg` | 水平物块的速度变化与理论值在 0.1 数量级内一致 |
| 静止接触 | 支持力与重力平衡 | 小球在水平地面稳定于半径高度，竖直速度小于 `0.01 m/s` |
| 曲面接触 | 圆弧/贝塞尔离散面不下沉、不穿透 | 小球在最低点持续 3 秒后仍稳定接触 |
| 无摩擦圆弧 | 持续接触时机械能守恒 | 小球运动 12 秒后的机械能漂移小于可用机械能的 `0.5%` |
| 圆轨道脱离 | 单侧支持力不能拉住物体 | 完整圆轨道顶部的低速小球在低于临界速度时离开圆周，后续不被重新投影回轨道 |
| 圆弧持续约束 | 不因折线/解析阈值切换产生假冲量 | `R=3 m`、球半径 `0.5 m`、`v=8 m/s` 的无重力完整圆周中，加速度与 `25.6 m/s²` 的误差小于 `1%`，接触状态不闪烁 |
| 曲面接触资格 | 只由真实接触激活并按支持力延续 | 主动分离、所需支持力为负、未连接端点或非法偏置曲率立即解除；预测接触不能重新激活约束 |
| 曲线精度 | 圆弧弦高误差、贝塞尔平坦度误差受控 | 默认最大几何误差 `0.002 m`，最大线段长度 `0.1 m` |
| 地面显示隔离 | 显示路径与碰撞采样相互独立 | 先绘制直线再绘制圆弧时二者之间无显示连线，也不产生额外碰撞面 |
| 双面地面碰撞 | 一个原生折线碰撞体同时阻挡正反两侧 | 小球从同一地面的两侧接近都发生碰撞；每块地面只创建一个 Rapier `polyline` 碰撞体 |
| 地面连接有效性 | 两个非退化语义端点可以相隔一定距离 | 每端点只被引用一次；普通角度只接受紧凑的安全五次过渡，`≥179°` 的分离端点只生成端点间直线；重合反向端点和切线连续的直线—曲线/曲线—曲线建立无形直接接缝 |
| 地面接缝交接 | 可见五次过渡不产生接缝冲量或折线反射 | 无摩擦小球在有/无重力条件下以多种速度从水平直线进入 `2°`、`15°`、`90°` 圆弧连接，离面误差小于 `0.02 m`、单步速度转角小于 `0.25 rad` 且球心偏置路径不出现曲率奇点；原有 `90°`、`150°` 双向速度跳变小于 `0.5%`，极端角不得生成发卡或环形路径 |
| 高速曲面过渡 | 曲率、位移子步与跨段时间保持统一 | 半径 `12.5 m`、球半径 `0.2/0.5/1 m`、速度 `20/25/26/30/40 m/s` 在有/无重力下不得于有效过渡或无形接缝弹起或反向；路径误差小于 `0.003 m`，速度或机械能漂移小于 `0.5%` |
| 连接地面高速斜碰 | 连接关系不能改变非接缝区域的接触法线 | 同一无摩擦完全弹性斜碰在有/无连接点时得到匹配速度并保持前进方向；覆盖远离过渡段、`30°～150°` 连接和双面入射 |
| 完全弹性曲面能量 | 固定无摩擦地面只改变速度方向，外力按实际位移做功 | 普通/连接的倾斜直线与圆弧单次碰撞机械能跳变小于 `0.2%`；无外力过渡段双面斜碰速率误差小于 `0.2%` |
| 物块接缝接触 | 曲线采样内部端帽不产生横向假冲量 | `0.4 m × 0.4 m` 物块以 `4 m/s` 双向通过无摩擦 `90°` 连接时，最大单步加速度小于 `100 m/s²`，最低速率大于 `3.5 m/s` |
| 表面吸附 | 移动对象的真实碰撞轮廓、正确方向支撑距离和空白侧法线 | 非对称预设与直线、圆弧、贝塞尔地面及布尔外环/孔洞真实相切；沿地面切线远离端点后不吸附，候选凸片仍实质穿透时拒绝，深层重叠不瞬移；隐藏、自身和来源树排除，`Alt` 绕过 |
| CCD | 高速物体连续碰撞检测 | `100 m/s` 小球即使未手动开启 CCD，也会在单步位移达到半径阈值时自动启用且不穿过零厚度直线地面 |
| 布尔几何 | 有序并集/差集、孔洞、多岛、嵌套结果和 Float32 凸片规范化 | 圆—圆、圆—矩形、旋转矩形、相离、相切、近相切、包含、完全切空、内切圆孔和深层嵌套均得到确定性轮廓；Float32 坐标量化位移不超过世界特征容差，量化面积漂移不超过按真实边界周长与实际位移计算的上界，去重、共线清理和逆时针化实际丢弃的面积不超过 `max(特征容差², 区域面积 × 1e-7)` |
| 钢笔物块几何 | 闭合贝塞尔轮廓与布尔复合几何共用面积和碰撞接口 | 凸形、凹形均按确定性误差采样并得到正确面积、质心和极惯量；自交、零面积、非有限、重复退化和超过 2048 个采样顶点的轮廓不生成实体；编辑后重定位局部节点时世界轮廓与外部锚点不跳动 |
| 物块预设与节点模式 | 曲面预设生成真实贝塞尔轮廓，折叠节点可精确恢复 | 三种曲面预设和 `5°～85°` 三角形得到确定性面积、质心、惯量与碰撞轮廓；物块和有限场节点折叠后控制棒为零，移动锚点不改变保存偏移，再次切换精确恢复，拖出控制棒退出折叠 |
| 钢笔物块碰撞 | 凹形刚体只暴露真实外边界，不暴露三角内部拼接边 | 钢笔物块与地面、小球、物块和连接器接触时使用共享索引网格；高速 CCD 不穿透，无摩擦无外部做功时不凭空增能，网格失败只停用对应实体并给出诊断 |
| 布尔质量属性 | 面密度积分、质心、极惯量和平行轴定理 | 不同质量相邻矩形的总质量、质心和惯量与解析解一致；挖孔同步移除对应质量和电荷，结果位置为派生质心 |
| 布尔独立分布覆盖 | 质量和电荷可分别采用来源密度或结果级均匀密度 | 未覆盖时保持来源非均匀分布；只覆盖质量不改变电荷区域，只覆盖电荷不改变质量区域；均匀模式按真实 CSG 面积排除孔洞并覆盖多岛；嵌套节点先输出自身分布，父节点同项覆盖时以父节点为准 |
| 布尔复合碰撞 | 共享索引网格、真实边界持续接触与专用曲面接触带 | 共享 Float32 顶点、三角方向、面积和外边界一致；加法与减法重叠体在无摩擦水平面运行 2400 步，高速不同材质加法体水平速度相对漂移 `<0.2%`，稳定阶段离地范围 `<0.01 m`、竖直速度 `<0.05 m/s` 且不自发旋转；小球在布尔平直顶面静止或以 `1 m/s` 滑行 360 步时穿入和高度误差 `<0.003 m`、静止横向漂移 `<0.01 m`、滑行机械能摆幅 `<0.01 J`；大跨度挖孔结果落入完整圆弧时真实边界越界 `<0.01 m`；块球加法体首次落弧后持续位移并转动；小球沿块减圆半圆槽运行 600 步时径向误差 `<0.01 m`，不得连续退化为自由落体；无摩擦圆槽往复 1440 步的机械能摆幅小于可转换机械能的 `0.5%`，主动离轨不回吸；有限质量平底布尔圆槽在地面上往复 2400 步时系统机械能摆幅和首末转折高度误差均 `<0.5%`、最低实体边界不低于地面 `0.002 m` 以上的容差下限、总水平动量误差 `<0.01 kg·m/s`，多点支撑不得被小球压着旋转穿地；小球从空中落入固定或有限质量半圆槽时，零弹性首次撞击只耗能，接触后加速度不再出现重复撞面尖峰，有限质量双方总能量不增加且总水平动量误差 `<0.01 kg·m/s`；有限质量槽体与小球接触时总线动量守恒、无摩擦总动能误差 `<1%`，摩擦只耗散且双方获得等大反向响应；高速 CCD、绳和自由端弹簧仍正确接触 |
| 布尔邻近无接触 | 编辑吸附不得泄漏到运行时碰撞 | 普通物块与布尔结果保持 `0.06 m` 间隙运行 600 固定步时不产生提前冲量、异常速度或机械能增长 |
| 布尔尖点离轨 | 圆弧偏置球心轨迹不得跨越实体续接到反向外壁 | 地面支撑的嵌套布尔斜坡在重力下以 `9 m/s` 接收小球；小球越过圆弧末端后保持向上的切线速度，横向速度 `<0.3 m/s`、总速率不超过初速 `0.01 m/s`，峰值加速度 `<45 m/s²`，不得被瞬移到外壁另一侧 |
| 可移动四分之一圆滑道 | 贝塞尔预设的解析圆弧、球心路径和滑道水平自由度共同守住连续加速度与动量 | `1 kg` 小球以 `15 m/s` 沿无摩擦、零弹性的 `5 kg` 四分之一圆滑道运动 720 个固定步：系统水平动量误差 `<0.01 kg·m/s`，圆弧内部相邻固定步加速度模长跳变 `<8 m/s²`；跨过竖直切线端点时先投影到精确解析端点再离轨，双方水平速度差 `<1e-6 m/s`（共同速度约 `2.5 m/s`）。小球自由落体返回尖端后重新进入圆弧，相对速度保留率 `>98%`，不得把完整圆采样接缝当成端点，也不得被折线端点法线吸附或减速 |
| 布尔属性优先级 | 加法重叠区域采用视觉上方输入属性 | 不同密度、摩擦、弹性、电荷和同类有限场强的重叠测试均只保留上方属性，下方只贡献未覆盖区域 |
| 可复现性 | 相同输入和步数得到相同输出 | 两个独立物理世界运行 240 步后的完整状态严格相等 |
| 场区域 | 旋转矩形、圆/扇形、自由多边形包含判断 | 内部、外部和边界点均按预期分类；正负圆心角方向正确 |
| 匀强电场 | `a = qE/m` | 运行 1 秒后的二维速度与解析值在 4 位小数内一致 |
| 匀强磁场 | `r = mv/|qB|`，磁场不做功 | 四分之一周期后位置符合理论半径，速率在 8 位小数内不变 |
| 点电荷对 | 库仑定律、牛顿第三定律 | 同号排斥；两物体的力和速度变化大小相等、方向相反 |
| 布尔分布力 | 每个保留质量/电荷区域在自身质心受力 | 电场产生正确合力与力矩；电荷区域对之间库仑力等大反向并保留两侧力矩；有限布尔场只在保留区域作用 |
| 复合磁场更新 | 平动与转动三自由度隐式中点 | 非均匀电荷复合体在匀强磁场中可发生耦合转动，但总动能不随固定步累计增加或减少 |
| 单摆 | `T ≈ 2π√(L/g)` | 小角度运行一个理论周期后回到初始侧，绳长不超限 |
| 绳 | 只限制最大长度 | 端点向外运动后距离不超过最大长度 `0.01 m` |
| 物体旋转锁 | Rapier 刚体转动自由度约束 | 小球和物块在非零初角速度、偏心弹簧、摩擦曲面和碰撞条件下角度漂移小于 `1e-6 rad`，角速度与转动动能始终为 `0` |
| 杆端旋转组合 | A/B 两端分别为自由铰或固定安装 | 两端自由、仅 A 固定、仅 B 固定、两端固定四种组合均保持杆长；固定端保持杆相对所附实体的方向 |
| 自由端杆 | 双边锚点距离约束 | 拉伸、压缩、不同质量、非中心锚点、共享物体、链和闭环中杆长误差小于 `1e-5 m` |
| 自由端杆长期稳定性 | 理想约束不做功并守恒动量 | 无外力双体杆运行 `300 s`，总机械能相对漂移小于 `0.5%`，质心速度和总角动量保持不变 |
| 固定端杆 | Rapier Fixed Joint 的完整焊接语义 | 保持配置长度、初始相对角度和完整相对位姿；双自由物体可整体旋转，任一端锁转时刚性组合停止整体旋转 |
| 弹簧振子周期 | 双质量无阻尼解析周期 | 运行一个解析周期后相对距离回到初始值的规定容差内 |
| 无阻尼弹簧长期稳定性 | 速度 Verlet 的能量误差有界 | 两个等质量自由小球以 `k=20 N/m` 运行 `300 s`，总机械能相对偏差小于 `0.5%`，前后半程速度峰值无持续增长或衰减，总动量和质心速度保持不变 |
| 弹簧阻尼 | 阻尼只能消耗锚点相对运动能量 | 非零阻尼全程总机械能不超过初始值并按预期衰减；阻尼 `0` 不施加任何隐式耗散 |
| 高刚度弹簧 | 自动子步满足 `ωΔt_sub ≤ 0.1` | `k=10000 N/m` 时状态有限、周期合理、能量偏差小于 `0.5%`；超过 32 子步能力时限制运行时有效刚度并产生可见警告 |
| 连接器端点 | 物体、地面、连接段、固定世界点和弹簧自由端使用明确约束语义 | 绳/杆可连接物体或地面；弹簧点击空白默认建立自由端，世界固定点需显式设置；解除、拖动和修改原长均不得意外移动另一端或把当前长度规范到原长 |
| 锚点边界 | 绳/杆夹紧、弹簧脱离 | 小球和旋转物块的绳/杆锚点不能移出碰撞轮廓；弹簧越界后转换为同一世界坐标，并可重新连接 |
| 碰撞绳 | 持续多段接触、单边释放，以及统一 XPBD 逐段、长距离和全绳弧长约束 | 质量为 `0.001/0.005/0.01/1 kg` 时，每段和总弧长均不得持续伸长；地面锚定摆绳扫过第三方圆球时至少三个相邻绳段形成包络，接触法线覆盖圆心角不小于 `0.25 rad`，并能绕到另一侧；微小数值重叠但已沿法向分离的绳段不得继续作为活动接触；内部节点单独弯曲时不得向静止端点注入速度；无摩擦绕球场景中包含绳节点重力势能的总机械能不得增加超过 `0.5%`；地面锚点单摆能越过最低点且连续摆动中单步速度变化小于 `0.2 m/s`、径向分离速度小于 `0.05 m/s`；总弧长误差不超过 `max(1e-4 m, maxLength × 1e-4)` |
| 连接器连续碰撞 | 连续胶囊扫掠、位置投影和接触冲量传递 | 碰撞绳与杆可阻挡小球、旋转物块和双面地面；`100 m/s` 内不得穿到错误一侧，残余穿透不超过 `0.002 m`；每次长度投影后必须重新扫掠实际移动绳段，并以接触投影结束；持续绕过圆球和旋转物块时逐帧保持在进入侧 |
| 弹簧自由端帽 | 单一压缩坐标和势能 `1/2 kc²` | 单端跟随锚点平移/旋转，双端围绕固定中点联动；停止态始终保留场景端点，首个物理步才把伸长端帽回拉到原长或释放预压端帽，预压展开时可把挡板前的物体顶出；小球、旋转物块和四类双面地面均只在可见挡板轴向接触，侧向经过不触发、压缩到底不穿透；无阻尼机械能误差低于 `0.5%` |
| 图表派生量 | `a = Δv/Δt`、`Eₖ = 1/2mv² + 1/2Iω²` | 加速度、合外力与重力解析值一致，总动能等于平动与转动动能之和 |
| 场景树语义 | 隐藏/锁定只影响编辑显示 | 隐藏并锁定节点后，物体仍按相同重力规律运动；根顺序不改变物理计算 |

对应测试位于：

- `src/physics/core/SimulationWorld.test.ts`
- `src/physics/core/groundCollisionChains.test.ts`
- `src/physics/core/groundSampling.test.ts`
- `src/physics/core/fieldRegions.test.ts`
- `src/physics/core/forces.test.ts`
- `src/editor/geometry/entityGeometry.test.ts`
- `src/scene/model/scene.test.ts`

测试数量以当前 `pnpm check` 和 `pnpm e2e` 的实际输出为准，本文不固定填写会随功能增长而过期的总数。

## 3. 数值实现约定

- 世界自身重力设为零，匀强重力场以 `F = mg` 的形式逐步施加，因此不同质量物体得到相同加速度；
- 匀强电场按 `F = qE` 累加；无电荷物体不受电场影响；
- 二维磁场按 `Fx = q·vy·Bz`、`Fy = -q·vx·Bz` 确定方向，并用精确速度旋转保持磁场不做功；
- 新建小球和物块的电荷量为 `0 C`，旧场景已有电荷值不被覆盖；所有带电物体使用 `k = 8.9875517923×10⁹ N·m²/C²` 成对计算，最小计算距离取两物体包围半径之和，新场景默认关闭物体间静电作用；
- 关闭旋转的物体使用 Rapier 转动自由度锁，并在创建和所有自定义角速度写入处显式归零；曲面摩擦把其逆转动惯量视为零，只改变平动速度；
- 关闭碰撞的零质量绳使用 Rapier 最大长度约束；开启碰撞的绳使用至少 `0.001 kg` 的动态节点，并在每个子步交替执行逐段长度、双向长距离张力、整绳弧长、连续接触和速度投影；长距离张力的位置与速度约束使用同一上限，避免单摆端点先径向外窜、随后被突然拉回；杆的 A/B 端分别按 Revolute/Fixed 语义约束；
- 双端连接的零质量弹簧继续使用步首/步末各半次冲量的二阶速度 Verlet；存在自由端时使用单一轴向压缩坐标和对称半冲量，不创建带质量柔性节点或普通碰撞体；
- 自由端杆每个内部子步最多执行 8 轮非线性位置 Gauss–Seidel 和 8 轮速度冲量求解；位置投影造成的离散角动量偏差在同一杆网络的约束零空间内恢复，不改变线动量或杆长；
- 弹簧的局部锚点包含角速度和转动惯量。每条弹簧以保守有效逆质量估算固有频率，整个世界统一使用满足 `ωΔt_sub ≤ 0.1` 的内部子步；默认上限为 32，超限只调整运行时有效刚度并发出警告，不修改场景数据；
- 活动曲面路径还按最大切线转角 `0.025 rad` 和球心位移不超过半径 `1/4` 计算统一内部子步；弧长推进使用精确段边界时间，跨段后继续消耗本步剩余时间；
- 碰撞绳和杆使用运行折线生成的连续胶囊段；这些段只进入自定义连续接触求解。碰撞绳按实际绳段维护持续接触流形，圆周上方向不同的接触会同时参与局部位置约束；同一 XPBD 批次交替处理物体/地面接触、逐段长度、双向长距离和总弧长。绳长闭合前保存绳段，闭合后只扫掠实际移动段，再以物体和地面接触投影收尾；速度重建前不再追加脱离接触求解器的完整绳长投影。纯地面接触用内部节点完成总长闭合，刚体接触则由逐段/长距离约束和真实端点有效质量协同传递反作用；若 32 轮后仍无共同可行解，最多执行四次纯接触投影并优先保持非穿透。随后只重建一次节点速度并执行法向、摩擦与拉伸速度投影。新接触的材料弹性只执行一次，纯长度投影发现的接触只阻止穿透。地面锚点只豁免锚点半径内的自身路径重叠，绳身远处仍可撞击同一路径；只有双固定端、满长度绳使用唯一合法的解析直线投影；
- 弹簧普通碰撞固定关闭。自由端横向挡板与开启碰撞的小球、物块以及直线、圆弧、贝塞尔和连接过渡地面发生轴向连续接触，其半宽不参与轴向提前量；停止态总是显示场景坐标，首个物理步才切换到运行原长。伸长端帽回拉，预压端帽展开并把储能传给接触物体，场景和撤销历史均不被改写；
- 阻尼系数 `0` 完全关闭阻尼，不设置隐藏最小值，不裁剪速度或能量；
- 每步显示的合外力由同一物理步的动量变化 `m·Δv/Δt` 得到，包含碰撞和约束的平均效果；
- 图表加速度直接使用上述合外力除以质量；动能使用 Rapier 的实际质量和有效转动惯量，不使用渲染帧差分；
- 图表按模拟时间采样，默认频率为 `60 Hz`；Worker 把多个样本批量发给主线程，因此 4× 倍速不会降低每模拟秒的样本数；
- 物体质量直接写入碰撞体，由 Rapier 根据形状和质量自动计算转动惯量；
- UI 摩擦系数先存为 `sqrt(μ)`，再使用 Rapier 的乘法合成规则，从而让接触对得到 `sqrt(μ₁ × μ₂)`；
- UI 弹性系数先存为 `sqrt(e)`，再使用 Rapier 的乘法合成规则，从而让接触对得到 `sqrt(e₁ × e₂)`；持续曲面接触资格判断使用同一公式；
- 布尔物体未编辑摩擦或弹性时，各保留材料区域继续使用来源属性；首次编辑某一项后，该项统一应用到全部材料网格，另一项仍保持原来源分区。初速度和初角速度同样独立从递归上方输入切换为结果整体覆盖，不修改来源实体原值；
- 圆弧按弦高误差自适应细分，贝塞尔曲线按控制点到弦的距离递归细分；
- 直线、解析圆弧、三次贝塞尔和连接过渡统一实现为可查询弧长、位置、切线、法线、曲率与最近点的地面路径；显示、碰撞和持续接触共用同一解析网络；
- 连接关系裁剪直线地面时，碰撞采样仍只输出解析起点和终点，不按曲线段长阈值制造共线顶点；这保证远离过渡段的斜碰不会仅因存在连接关系而得到不同法线；
- 开启碰撞的小球由真实 Rapier 接触进入持久路径状态。活动路径碰撞对保留接触流形但关闭 Rapier 折线冲量，沿弧长推进并跨越相邻过渡；物块及其他碰撞对继续由 Rapier 求解；
- 完全弹性小球初次撞击带连接关系的地面网络时，求解器把同一解析网络产生的重复折线接触合并到最接近的真实解析接触，避免人工顶点法线反转切向分量；
- 组合弹性系数为 `1` 且摩擦为 `0` 时，步末仍存在的 Rapier 接触优先使用解析接触法线；若 CCD 或离散求解已让接触在步末分离，则沿步首自由轨迹扫掠解析碰撞片并二分首次相交时刻，避免漏掉短暂碰撞；
- 完全弹性碰撞只反转解析接触法向速度；扫掠得到首次撞击时刻时，剩余外力继续沿原方向作用。步末仍有多流形接触时，以最近解析接触法线为权威，避免双面过渡片选错法线；所有分支的最终速率均按 `v_out² = v_in² + 2a·Δx` 与当前步实际位移校准，因此固定地面不注入或吸收机械能。摩擦不为零或组合弹性系数小于 `1` 时仍由既有摩擦与 Rapier 恢复系数模型处理，不套用守恒校准；
- 物块使用同一解析地面网络生成的极薄双面接触带。相同材质的相邻地面和连接过渡先拼成连续链，共线内部采样点会被删除；链包围盒先排除当前步内不可能接近的地面，每个物块只让最近线段及其两侧相邻线段参与当前步求解，内部端帽则延伸到物块包围半径之外；
- 持续路径状态不依赖每帧预测接触。主动分离速度会先扣除本步已知外力造成的法向速度增量，避免把本应由正支持力抵消的重力或场力误判为离轨；所需支持力为负、真实主动分离、走出未连接端点或偏置路径无物理解时仍立即脱离，重新进入必须再次满足真实接近接触；
- 路径摩擦按支持力限制接触点切向冲量，并使用刚体有效逆质量和有效转动惯量耦合平动与转动；摩擦为零时不施加耗散冲量；
- 每个地面显示实体使用独立子路径，圆弧从自身解析起点开始绘制；该显示规则不改变曲线碰撞采样，也不会创建隐藏碰撞线；
- 直线、圆弧和贝塞尔地面都先得到有序采样点。小球使用 Rapier 原生双面 `polyline`，不复制反向碰撞体；物块使用厚度仅 `0.001 m` 的双面接触带，其表面相对解析路径的偏移为 `0.0005 m`，低于既有 `0.002 m` 几何误差预算；
- `GroundJointEntity` 的 `a`、`b` 保存语义端点引用，并保留自动/手动过渡长度。两个端点可以相隔一定距离，但必须来自不同非退化地面且未被其他连接占用；
- `≤1°` 的连接失效；`1°～179°` 只接受通过有限导数、曲率、长度包络、累计转角和无自交检查的五次过渡；`≥179°` 直接使用端点间直线。旧 `directionFlipped` 数据保留但不再参与求解；
- 有效五次连接裁剪两侧地面并生成位置、切线和曲率连续的可见过渡。多个安全候选按端点曲率范围外偏移、额外曲率变化、最大绝对曲率和弯曲能依次比较，避免“第一个有效解”在连接中段产生可见折线运动；连接点只消除几何接缝假冲量，不会在支持力为负时把小球强制留在轨道上；
- 地面被移动或编辑后若连接不再满足条件，连接点以红色显示，物理层完全忽略其平滑交接语义；
- 所有开启碰撞的小球均可使用持续路径约束；非零摩擦由路径接触冲量处理。物块与关闭碰撞的小球不进入路径状态；
- 墙面吸附使用鼠标所控制的普通或布尔物体中心到折线最近点的真实欧氏距离，并以同一分离方向的真实外轮廓支撑半径计算表面间隙；物块吸附还允许普通物体与有效可见根布尔结果双向吸附。布尔目标遍历外环、孔洞和多岛边界，按点在实体内外选择空白侧法线；移动布尔结果自身的支撑忽略孔洞。自身、整棵来源子树和正在编辑来源所属根结果排除，全部后代接受同一原子修正；
- 物理线程最多追赶 16 个固定步，设备长期跟不上时会丢弃过期追赶量并发出警告，避免界面永久卡死。

## 4. 已知边界

- 这是实时数值模拟，不是符号计算器；长时间、高刚度、极端质量比和大量高速碰撞仍会积累数值误差；
- 超出 32 个内部子步能力的弹簧会在当前模拟中使用明确告知的较低有效刚度；这是实时稳定性边界，不会静默篡改存档；
- Rapier 的一般圆弧/贝塞尔碰撞仍是误差受控的折线近似；开启碰撞的小球在确认真实接触后使用统一解析路径维持持续接触；
- 物块在曲率半径接近自身尺寸、尖锐折返或同时接触多条交叉地面时仍属于通用刚体离散接触问题；局部接触带消除连接内部端帽假冲量，但不把物块强制约束到曲线；
- 圆弧、贝塞尔、小球和控制点的渲染分段与平滑度保持现状；随缩放自适应平滑度和曲线质量设置延期，不属于本次物理修复；
- 手动地面连接没有端点距离上限，但安全五次曲线求解可能明确失败；`≥179°` 的直线退化连接优先避免绕圈，不保证与两侧地面切线连续；
- 点电荷成对计算目前为 `O(n²)`，适合教学场景中的少量电荷；大量电荷的空间加速算法留待性能数据证明有需要时实现；
- 自由端杆的非中心锚点和多重闭环使用固定 8 轮迭代；已覆盖常见不同质量、链和三角闭环，极端质量比或超大型闭环机构仍可能需要更小的固定步长；
- 同一步内同时存在强电场、强磁场和碰撞时采用固定步长分裂推进，仍有 `Δt` 量级的数值误差；
- 图表最多 8 个坐标系、每图 12 条线、全场景 32 条线和 20 个被记录物体；共享缓冲区默认按模拟时间记录 60 样本/秒并循环保留最近 300 秒。图表抽样只影响显示，公式重算和 CSV 仍使用完整记录；画布轨迹独立保留最近 1800 个渲染采样点。

任何物理核心修改都必须重新运行 `pnpm test` 和 `pnpm e2e`，并为新规律或新边界条件增加测试。
